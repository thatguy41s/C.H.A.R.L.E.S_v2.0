[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  # This tells Netlify to use the modern bundler without the invalid 'true' syntax
  node_bundler = "esbuild"

[[edge_functions]]
  path = "/.netlify/functions/*"
  function = "charles"
  
